services:
  hugo:
    build: .
    working_dir: /src
    volumes:
      - .:/src
    ports:
      - "1313:1313"
    command:
      - hugo
      - server
      - --bind
      - 0.0.0.0
      - --baseURL
      - http://localhost:1313
      - --disableFastRender
